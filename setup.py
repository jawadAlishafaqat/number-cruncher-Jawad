"""Minimal setup.py for editable installs with legacy tools.

All project metadata lives in pyproject.toml; this file only exists for
compatibility with tools that do not yet support PEP 517/518 fully.
"""

from setuptools import setup

setup()
